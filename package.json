{
  "name": "quotation-management-system",
  "version": "1.0.0",
  "description": "A robust quotation management system with React, Node.js, and PostgreSQL",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "db:init": "cd server && node scripts/initDb.js",
    "db:seed": "cd server && node scripts/seedDb.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
